import React from 'react';
import { disableBodyScroll, enableBodyScroll, clearAllBodyScrollLocks } from 'body-scroll-lock';
export default function useBodyScrollLock(ref, locked) {
    if (locked === void 0) { locked = false; }
    React.useEffect(function () {
        if (!ref.current)
            return;
        var fn = locked ? disableBodyScroll : enableBodyScroll;
        fn(ref.current);
    }, [ref, locked]);
    React.useEffect(function () {
        function onUnmount() {
            clearAllBodyScrollLocks();
        }
        return onUnmount;
    }, []);
}
//# sourceMappingURL=use-body-scroll-lock.js.map