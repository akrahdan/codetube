
import React, { forwardRef } from 'react'

import Icon, { IconComponent } from '..'

const PlayIcon = forwardRef((props, ref) => {
  const { 'aria-label': ariaLabel, ...rest } = props
  return (
    <Icon {...rest} ref={ref}>
      <svg aria-label="play icon" viewBox="0 0 24 24"role='img' {...(ariaLabel && { 'aria-label': ariaLabel })}><path fillRule="evenodd" clipRule="evenodd" d="M8.77 19.508L20 12.342c.246-.157.254-.68 0-.842L8.77 4.493c-.28-.18-.77.086-.77.42v14.174c0 .332.488.601.77.42z"/></svg>
    </Icon>
  )
}) as IconComponent

PlayIcon.displayName = "PlayIcon"

PlayIcon.colors = Icon.colors
PlayIcon.sizes = Icon.sizes

export { PlayIcon  }
