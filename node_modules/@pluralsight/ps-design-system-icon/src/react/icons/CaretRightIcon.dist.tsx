
import React, { forwardRef } from 'react'

import Icon, { IconComponent } from '..'

const CaretRightIcon = forwardRef((props, ref) => {
  const { 'aria-label': ariaLabel, ...rest } = props
  return (
    <Icon {...rest} ref={ref}>
      <svg aria-label="caret right icon" viewBox="0 0 24 24"role='img' {...(ariaLabel && { 'aria-label': ariaLabel })}><path d="M16 12l-5-5-1.41 1.41L13.17 12l-3.58 3.59L11 17"/></svg>
    </Icon>
  )
}) as IconComponent

CaretRightIcon.displayName = "CaretRightIcon"

CaretRightIcon.colors = Icon.colors
CaretRightIcon.sizes = Icon.sizes

export { CaretRightIcon  }
