
import React, { forwardRef } from 'react'

import Icon, { IconComponent } from '..'

const MoreIcon = forwardRef((props, ref) => {
  const { 'aria-label': ariaLabel, ...rest } = props
  return (
    <Icon {...rest} ref={ref}>
      <svg aria-label="more icon" viewBox="0 0 24 24"role='img' {...(ariaLabel && { 'aria-label': ariaLabel })}><path d="M6 14.5a2 2 0 110-4 2 2 0 010 4zm12 0a2 2 0 110-4 2 2 0 010 4zm-6 0a2 2 0 110-4 2 2 0 010 4z"/></svg>
    </Icon>
  )
}) as IconComponent

MoreIcon.displayName = "MoreIcon"

MoreIcon.colors = Icon.colors
MoreIcon.sizes = Icon.sizes

export { MoreIcon  }
