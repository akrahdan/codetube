import { ValueOf } from '@pluralsight/ps-design-system-util';
import React from 'react';
import { statuses } from '../../vars/index';
interface StoryStep {
    description: string;
    status: ValueOf<typeof statuses>;
    title: string;
}
export declare function useStoryData(): {
    steps: StoryStep[];
    selectStep: (nextStep: StoryStep) => void;
};
export declare const FixedWidthContainer: React.FC<{
    width?: number;
}>;
export {};
