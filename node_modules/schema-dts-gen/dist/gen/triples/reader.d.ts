import { Observable } from 'rxjs';
import { Triple } from './triple';
export declare function toTripleStrings(data: string[]): string[][];
/**
 * Loads schema all Triples from a given Schema file and version.
 */
export declare function load(url: string): Observable<Triple>;
/**
 * does the same as load(), but for a local file
 */
export declare function loadFile(path: string): Observable<Triple>;
export declare function process(triples: string[][]): Iterable<Triple>;
//# sourceMappingURL=reader.d.ts.map