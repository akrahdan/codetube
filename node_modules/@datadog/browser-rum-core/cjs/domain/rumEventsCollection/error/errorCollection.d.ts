import { Configuration, Context, ClocksState } from '@datadog/browser-core';
import { CommonContext } from '../../../rawRumEvent.types';
import { LifeCycle } from '../../lifeCycle';
import { ForegroundContexts } from '../../foregroundContexts';
export interface ProvidedError {
    startClocks: ClocksState;
    error: unknown;
    context?: Context;
    source: ProvidedSource;
    handlingStack: string;
}
export declare type ProvidedSource = 'custom' | 'network' | 'source';
export declare function startErrorCollection(lifeCycle: LifeCycle, configuration: Configuration, foregroundContexts: ForegroundContexts): {
    addError: ({ error, handlingStack, startClocks, context: customerContext, source }: ProvidedError, savedCommonContext?: CommonContext | undefined) => void;
};
export declare function doStartErrorCollection(lifeCycle: LifeCycle, foregroundContexts: ForegroundContexts): {
    addError: ({ error, handlingStack, startClocks, context: customerContext, source }: ProvidedError, savedCommonContext?: CommonContext | undefined) => void;
};
