import React from 'react';
export declare type ToggleSizes = keyof typeof sizes;
export declare type ToggleVariants = typeof colors[number];
export declare type LabelProps = {
    disabled?: boolean;
    variant?: ToggleVariants;
};
export declare type ToggleProps = {
    /** The state of the checkbox input (this can be out of sync with the input's value if not passed) */
    checked: boolean;
    /** Called when the input value has changed */
    onChange: (event?: React.FormEvent<HTMLInputElement>) => void;
    /** A hidden label used for accessibility and control, unique to the page */
    label?: string;
    /** Color variations for background styles */
    variant?: ToggleVariants;
    /** Changes the dimensions of the element for using the component outside of a form context */
    size?: ToggleSizes;
    disabled?: boolean;
    className?: string;
};
declare const sizes: {
    medium: {
        height: string;
        width: string;
    };
    small: {
        height: string;
        width: string;
    };
};
declare const colors: readonly ["blue", "hyper"];
export declare const Toggle: React.FC<ToggleProps>;
export {};
