import React, { ComponentProps } from 'react';
import { Box } from '../Box';
export declare type PatternName = 'diagonalStripesLoose' | 'diagonalStripesRegular' | 'diagonalStripesDense' | 'checkerLoose' | 'checkerRegular' | 'checkerDense' | 'dotLoose';
export interface PatternProps extends ComponentProps<typeof Box> {
    /**
     * Unique ID suffix to add the pattern ID to remove page duplicates.
     */
    idSuffix?: string;
    name: PatternName;
}
export declare const Pattern: React.FC<PatternProps>;
