/* eslint-disable jsx-a11y/iframe-has-title */
import React from 'react';
import styles from './styles.module.scss';
var YOUTUBE_PATTERN = /youtu(be\.com|\.be)/;
var VIMEO_PATTERN = /player\.vimeo\.com/;
export var Iframe = function Iframe(props) {
  if (props.src && [YOUTUBE_PATTERN, VIMEO_PATTERN].some(function (pattern) {
    return pattern.test(props.src);
  })) {
    var _props$width = props.width,
        width = _props$width === void 0 ? 16 : _props$width,
        _props$height = props.height,
        height = _props$height === void 0 ? 9 : _props$height;
    var ratioPadding = (Math.round(height) / Math.round(width) * 100).toFixed(2);
    var wrapperStyles = {
      paddingBottom: "".concat(ratioPadding, "%")
    };
    return /*#__PURE__*/React.createElement("div", {
      className: styles.youtubeVideoWrapper,
      "data-testid": "yt-iframe",
      style: wrapperStyles
    }, /*#__PURE__*/React.createElement("iframe", props));
  }

  return /*#__PURE__*/React.createElement("iframe", props);
};