import { VideoProps } from '@codecademy/gamut';
import React from 'react';
import { BaseProps } from './types';
export declare type ImageProps = {
    src: string;
    alt: string;
};
export declare type MediaProps = ({
    type: 'image';
} & ImageProps) | ({
    type: 'video';
} & VideoProps);
export declare type PageHeroProps = BaseProps & {
    media?: MediaProps;
};
export declare const PageHero: React.FC<PageHeroProps>;
