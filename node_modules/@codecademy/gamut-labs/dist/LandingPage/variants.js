import { variant } from '@codecademy/gamut-styles';
export var darkModeVariants = variant({
  prop: 'mode',
  defaultVariant: 'light',
  variants: {
    dark: {
      textColor: 'beige'
    },
    light: {
      textColor: 'navy'
    }
  }
});