import { VisualTheme } from '@codecademy/gamut';
import React from 'react';
export declare type TestimonialProps = {
    firstName: string;
    lastName: string;
    occupation: string;
    quote: string;
    mode: VisualTheme;
    /**
     * Associated workplace or institution
     */
    company?: string;
    /**
     * An avatar portrait
     */
    imageUrl?: string;
    /**
     * A long quote to replace the text with at SM viewports and higher.
     */
    longQuote?: string;
    href?: string;
    onClick?: () => void;
};
export declare const Testimonial: React.FC<TestimonialProps>;
