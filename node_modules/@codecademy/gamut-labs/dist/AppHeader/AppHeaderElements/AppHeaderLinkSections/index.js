import { Box } from '@codecademy/gamut';
import React from 'react';
import { AppHeaderLinkMobile } from '../../../AppHeaderMobile/AppHeaderLinkMobile';
export var AppHeaderLinkSections = function AppHeaderLinkSections(_ref) {
  var action = _ref.action,
      item = _ref.item;
  return /*#__PURE__*/React.createElement(Box, null, item.type === 'profile-dropdown' ? item.popover.map(function (linkSection, sectionIndex) {
    return linkSection.map(function (link, linkIndex) {
      return /*#__PURE__*/React.createElement(AppHeaderLinkMobile, {
        action: action,
        item: link,
        key: link.id,
        topSeparator: sectionIndex !== 0 && linkIndex === 0
      });
    });
  }) : item.popover.map(function (link) {
    return /*#__PURE__*/React.createElement(AppHeaderLinkMobile, {
      action: action,
      item: link,
      key: link.id
    });
  }));
};